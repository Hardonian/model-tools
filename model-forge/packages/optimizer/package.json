{
  "name": "@modelforge/optimizer",
  "version": "1.0.0",
  "description": "Multi-objective AI inference workload optimization engine and manifest generator",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "@modelforge/hardware-registry": "workspace:*",
    "@modelforge/model-fit": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@modelforge/config": "workspace:*",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
