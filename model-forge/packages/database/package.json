{
  "name": "@modelforge/database",
  "version": "1.0.0",
  "description": "PostgreSQL database types, multi-tenant schemas, and local fallback data layer",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "@modelforge/benchmark-schema": "workspace:*",
    "@modelforge/hardware-registry": "workspace:*",
    "@modelforge/slo-compiler": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@modelforge/config": "workspace:*",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
