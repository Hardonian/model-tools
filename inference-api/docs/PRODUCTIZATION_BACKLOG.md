# Workstation Co-Operator Productization Backlog

## Highest ROI next

1. Install/demo packaging
   - one-command installer for dashboard + systemd units
   - demo mode with fake GPU/service data
   - migration check: ports, service names, model roots

2. Sales assets
   - 8 screenshots
   - 10-minute demo script
   - landing page headline: "A private command center for your chaotic local AI lab"
   - offer: $297 template / $499 setup / $29mo managed updates

3. Client audit checklist
   - disk pressure
   - duplicate model stores
   - GPU visibility
   - ComfyUI path truth
   - Ollama lane truth
   - service restart policy
   - smoke timer proof

4. Dashboard hardening
   - admin token UI flow
   - typed confirmation for destructive actions
   - CSRF guard for non-localhost if ever exposed
   - result cache for heavy scans

5. Model migration assistant
   - detect active paths
   - manifest before move
   - archive duplicates
   - never delete active stores automatically

## Kill criteria

Stop productization if nobody will pay after 10 direct outreach offers or if setup cannot be completed in under 45 minutes on a second machine.
